services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: --requirepass Onzo@1901
    restart: unless-stopped

volumes:
  redis_data: